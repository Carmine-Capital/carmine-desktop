{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "default",
  "description": "Default capabilities for CloudMount",
  "windows": ["wizard", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default",
    "dialog:allow-confirm"
  ]
}
