---
phase: 04-visual-polish-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/carminedesktop-app/dist/styles.css
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Design tokens in :root use the UI-SPEC color values (lifted dark palette)"
    - "--space-3 and --space-5 tokens are removed; all former usage sites remap to standard tokens"
    - "Typography uses 4-tier scale (11px, 13px, 16px, 32px) with 2 weights (400, 600)"
    - "Components (buttons, inputs, toggles, cards, sidebar, errors, badges) use updated specs"
    - "New transitions added to buttons and cards; prefers-reduced-motion covers all transitions"
  artifacts:
    - path: "crates/carminedesktop-app/dist/styles.css"
      provides: "Complete CSS design system with refreshed tokens and component specs"
      contains: "--bg-base.*#1e2030"
  key_links:
    - from: "styles.css :root tokens"
      to: "All component rules"
      via: "var() references"
      pattern: "var\\(--bg-base\\)|var\\(--space-4\\)|var\\(--radius-md\\)"
---

<objective>
Overhaul the CSS design system tokens and component rules in styles.css to deliver the soft dark palette refresh, consolidated typography, normalized spacing scale, and refined component polish specified in the UI-SPEC.

Purpose: This is the bulk of UI-01 -- the token cascade means changing :root values updates every component automatically. Component rule updates handle properties that don't cascade (padding, font-size, border-radius on specific selectors).

Output: Updated styles.css with all 04-UI-SPEC.md design contract values applied.
</objective>

<execution_context>
@/home/nyxa/.claude/get-shit-done/workflows/execute-plan.md
@/home/nyxa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-polish-hardening/04-UI-SPEC.md
@.planning/phases/04-visual-polish-hardening/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and remap removed spacing tokens</name>
  <files>crates/carminedesktop-app/dist/styles.css</files>
  <read_first>
    - crates/carminedesktop-app/dist/styles.css (current token values and all --space-3 / --space-5 usage sites)
    - .planning/phases/04-visual-polish-hardening/04-UI-SPEC.md (authoritative target values)
  </read_first>
  <action>
Update the `:root` block (Block 2, lines 20-50) with the following exact token values:

```css
:root {
  color-scheme: dark;
  --bg-base:        #1e2030;
  --bg-surface:     #242637;
  --bg-elevated:    #2a2d3e;
  --border:         rgba(255,255,255,0.08);
  --border-row:     rgba(255,255,255,0.05);
  --accent:         #99222E;
  --accent-hover:   #b52a38;
  --accent-bg:      rgba(153,34,46,0.80);
  --accent-glow:    0 0 16px rgba(153,34,46,0.15);
  --text-primary:   #e2e4f0;
  --text-secondary: #8e92b0;
  --text-muted:     #6e7298;
  --success:        #34d399;
  --danger:         #f87171;
  --warning:        #fbbf24;
  --space-1: 4px;
  --space-2: 8px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --shadow-sm:   0 1px 2px rgba(0,0,0,0.25);
  --shadow-md:   0 4px 12px rgba(0,0,0,0.35);
  --shadow-glow: var(--accent-glow);
}
```

Key changes from current:
- Remove `--space-3` (was 0.75rem) and `--space-5` (was 1.25rem) token declarations entirely
- All spacing values switch from rem to px
- Colors lifted per UI-SPEC (see each token's "New" column)
- Radii increased (sm: 4->6, md: 6->8, lg: 10->12)
- Shadows softened

Then remap all `--space-3` and `--space-5` references in component rules:
1. Line 113: `.btn-sm { padding: var(--space-1) var(--space-3); ... }` --> change `var(--space-3)` to `var(--space-4)`
2. Line 650: `.sp-back-link { margin-bottom: var(--space-3); ... }` --> change `var(--space-3)` to `var(--space-2)`

Update activity tag badge background rgba values to match new semantic token RGB values:
- `.activity-tag.synced, .activity-tag.uploaded`: change `rgba(34, 197, 94, 0.12)` to `rgba(52, 211, 153, 0.12)` (matches new --success #34d399)
- `.activity-tag.deleted`: change `rgba(239, 68, 68, 0.12)` to `rgba(248, 113, 113, 0.12)` (matches new --danger #f87171)
- `.activity-tag.conflict`: change `rgba(245, 158, 11, 0.12)` to `rgba(251, 191, 36, 0.12)` (matches new --warning #fbbf24)

Same for health badge backgrounds:
- `.health-badge.downloaded`: change `rgba(34, 197, 94, 0.12)` to `rgba(52, 211, 153, 0.12)`
- `.health-badge.partial`: change `rgba(245, 158, 11, 0.12)` to `rgba(251, 191, 36, 0.12)`
- `.health-badge.stale`: change `rgba(239, 68, 68, 0.12)` to `rgba(248, 113, 113, 0.12)`

Same for auth banner background:
- `.auth-banner`: change `rgba(245, 158, 11, 0.12)` to `rgba(251, 191, 36, 0.12)` and border `rgba(245, 158, 11, 0.3)` to `rgba(251, 191, 36, 0.3)`

Also update `.auth-countdown.warning` from `color: #f59e0b` to `color: var(--warning)` to use the token.
  </action>
  <verify>
    <automated>grep -c 'space-3\|space-5' crates/carminedesktop-app/dist/styles.css | grep -q '^0$' && grep -q '#1e2030' crates/carminedesktop-app/dist/styles.css && grep -q '#6e7298' crates/carminedesktop-app/dist/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - styles.css :root contains `--bg-base: #1e2030`
    - styles.css :root contains `--bg-surface: #242637`
    - styles.css :root contains `--bg-elevated: #2a2d3e`
    - styles.css :root contains `--text-muted: #6e7298`
    - styles.css :root contains `--success: #34d399`
    - styles.css :root contains `--danger: #f87171`
    - styles.css :root contains `--warning: #fbbf24`
    - styles.css :root contains `--radius-sm: 6px`
    - styles.css :root contains `--radius-md: 8px`
    - styles.css :root contains `--radius-lg: 12px`
    - styles.css :root contains `--shadow-sm: 0 1px 2px rgba(0,0,0,0.25)`
    - styles.css contains zero occurrences of `space-3` and zero occurrences of `space-5`
    - styles.css :root contains `--space-1: 4px` (not 0.25rem)
    - `.btn-sm` line uses `var(--space-4)` not `var(--space-3)`
    - `.sp-back-link` line uses `var(--space-2)` not `var(--space-3)`
    - `.activity-tag.synced` background uses `rgba(52, 211, 153, 0.12)`
    - `.health-badge.downloaded` background uses `rgba(52, 211, 153, 0.12)`
    - `.auth-banner` background uses `rgba(251, 191, 36, 0.12)`
  </acceptance_criteria>
  <done>All :root tokens match UI-SPEC values exactly. --space-3 and --space-5 are fully removed with zero remaining references. Badge/tag backgrounds use RGB values matching new semantic tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Update typography, component rules, and transitions</name>
  <files>crates/carminedesktop-app/dist/styles.css</files>
  <read_first>
    - crates/carminedesktop-app/dist/styles.css (after Task 1 token updates)
    - .planning/phases/04-visual-polish-hardening/04-UI-SPEC.md (component specs, typography, transitions)
  </read_first>
  <action>
Apply all component-level CSS rule changes from the UI-SPEC. These are updates to individual selectors -- the token cascade handled color/radius/shadow globally; these are padding, font-size, font-weight, and structural property changes.

**Buttons (Block 3):**
- `button` base: change `padding: 7px 18px` to `padding: 8px 16px`; `font-size: 12.5px` to `font-size: 13px`; `font-weight: 500` to `font-weight: 400`; add `box-shadow` to transition: `transition: background 0.15s ease, color 0.15s ease, box-shadow 0.15s ease, opacity 0.15s ease;`
- `.btn-ghost`: change `border: 1px solid rgba(255,255,255,0.10)` to `border: 1px solid var(--border)`
- `.btn-link`: change `font-size: 12px` to `font-size: 11px`
- `.btn-sm`: font-size stays `11px` (unchanged), padding already remapped to `var(--space-1) var(--space-4)` in Task 1

**Inputs & Selects (Block 4):**
- `input[type="text"], input[type="number"], select, .input`: change `border-radius: 5px` to `border-radius: var(--radius-md)`; `padding: 4px 10px` to `padding: 8px 16px`; `font-size: 11.5px` to `font-size: 13px`; `border: 1px solid rgba(255,255,255,0.10)` to `border: 1px solid var(--border)`

**Toggle Switches (Block 4):**
- `.toggle-track`: change `width: 30px; height: 16px` to `width: 32px; height: 18px`; change `background: var(--bg-elevated)` to `background: rgba(255,255,255,0.08)`
- `.toggle-track::after`: change `width: 12px; height: 12px` to `width: 14px; height: 14px`; change `background: var(--text-muted)` to `background: var(--text-secondary)`

**Sidebar (Block 5):**
- `.sidebar`: change `padding: 14px 10px` to `padding: 16px 16px`
- `.sidebar-header`: change `padding: 4px 8px 18px` to `padding: 4px 8px 24px`
- `.nav-item`: change `padding: 7px 12px` to `padding: 8px 16px`; `font-size: 12.5px` to `font-size: 13px`; `font-weight: 500` to `font-weight: 400`; add `box-shadow` to transition: `transition: background 0.15s ease, color 0.15s ease, box-shadow 0.15s ease;`
- `.sidebar-footer`: change `padding: 10px 12px` to `padding: 8px 16px`
- `.main-content`: change `padding: 22px 28px` to `padding: var(--space-8) var(--space-6)` (32px 24px)

**Section Headings + Setting Rows (Block 6):**
- `.section-heading`: change `margin: 22px 0 14px` to `margin: var(--space-6) 0 var(--space-4)` (24px 0 16px)
- `.setting-row`: change `padding: 9px 0` to `padding: 16px 0`

**Handlers (Block 7):**
- `.handler-ext`: change `font-size: 12.5px` to `font-size: 13px`
- `.handler-name`: change `font-size: 11.5px` to `font-size: 11px`

**Drive Cards (Dashboard, ~line 326-336):**
- `.drive-card`: change `padding: 12px` to `padding: var(--space-4)`; `gap: 4px` to `gap: 8px`; add `box-shadow: var(--shadow-sm)`; add `transition: border-color 0.15s ease, box-shadow 0.15s ease;`
- Add new rule: `.drive-card:hover { border-color: rgba(255,255,255,0.12); }`
- `.drive-card-name`: change `font-weight: 600` to `font-weight: 400`

**Status Dots (Dashboard):**
- `.status-dot`: add `box-shadow: 0 0 0 2px var(--bg-elevated);`

**Error Entries (Dashboard):**
- `.error-entry`: change `padding: 8px 16px` to `padding: 16px 16px`; add `box-shadow: var(--shadow-sm);`

**Cache Bar (Dashboard):**
- `.cache-bar`: change `border-radius: 4px` to `border-radius: var(--radius-sm)`; change `background: var(--bg-elevated)` to `background: rgba(255,255,255,0.06)`
- `.cache-bar-fill`: change `border-radius: 4px` to `border-radius: var(--radius-sm)`

**Health Badges + Activity Tags (Dashboard):**
- `.activity-tag`: change `border-radius: 3px` to `border-radius: var(--radius-sm)`
- `.health-badge`: change `border-radius: 3px` to `border-radius: var(--radius-sm)`

**Status Bar (#status-bar):**
- Change `font-size: 12px` to `font-size: 11px`; add `border-radius: var(--radius-md) var(--radius-md) 0 0;`

**Wizard-specific (Block 8):**
- `.stepper-item`: change `padding: 7px 12px` to `padding: 8px 16px`; `font-size: 12.5px` to `font-size: 13px`; `font-weight: 500` to `font-weight: 400`
- `.step h1.step-title-lg`: change `font-size: 18px` to `font-size: 16px`
- `.step-centered`: change `padding: 0 28px` to `padding: 0 var(--space-6)`
- `.step-scroll`: change `padding: 22px 28px` to `padding: var(--space-8) var(--space-6)`
- `.step .step-sub`: change `margin-bottom: 22px` to `margin-bottom: var(--space-6)`
- `.sp-back-link`: change `font-weight: 500` to `font-weight: 400`; `font-size: 12px` to `font-size: 11px`

**Prefers-Reduced-Motion:**
Update the `@media (prefers-reduced-motion: reduce)` blocks to cover newly added transitions:

```css
@media (prefers-reduced-motion: reduce) {
  .disclosure-arrow { transition: none; }
  .cache-bar-fill { transition: none; }
  button { transition: none; }
  .nav-item { transition: none; }
  .drive-card { transition: none; }
}
```

And the existing spinner block at line 587 stays unchanged.

**Add new CSS classes for Plan 02 inline style migration (append before Block 8 comment or in a dedicated section):**

```css
/* -- Inline style migration classes -- */
.handler-info { display: flex; align-items: center; gap: var(--space-2); }
.pin-file-count { font-size: 11px; color: var(--text-muted); margin-left: var(--space-1); }
.auth-banner-icon { color: var(--warning); }
.auth-status-row { display: flex; align-items: center; gap: var(--space-2); margin-bottom: var(--space-6); }
.cancel-link { margin-top: var(--space-6); }
.wizard-actions { margin-top: var(--space-6); display: flex; flex-direction: column; align-items: center; gap: var(--space-2); }
.done-mount-list { list-style: none; width: 100%; margin-bottom: var(--space-6); }
```

Also update `.hint`: change `margin-top: 10px` to `margin-top: var(--space-2)` (8px).
  </action>
  <verify>
    <automated>grep -q 'padding: 8px 16px' crates/carminedesktop-app/dist/styles.css && grep -q 'font-size: 13px' crates/carminedesktop-app/dist/styles.css && grep -q '\.handler-info' crates/carminedesktop-app/dist/styles.css && grep -q '\.drive-card:hover' crates/carminedesktop-app/dist/styles.css && grep -q 'prefers-reduced-motion' crates/carminedesktop-app/dist/styles.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `button` rule contains `padding: 8px 16px` and `font-size: 13px` and `font-weight: 400`
    - `button` transition includes `box-shadow`
    - `input[type="text"]` rule contains `padding: 8px 16px` and `font-size: 13px` and `border-radius: var(--radius-md)`
    - `.toggle-track` contains `width: 32px; height: 18px` and `background: rgba(255,255,255,0.08)`
    - `.toggle-track::after` contains `width: 14px; height: 14px` and `background: var(--text-secondary)`
    - `.sidebar` contains `padding: 16px 16px`
    - `.nav-item` contains `padding: 8px 16px` and `font-size: 13px` and `font-weight: 400`
    - `.main-content` contains `padding: var(--space-8) var(--space-6)`
    - `.section-heading` margin uses `var(--space-6)` and `var(--space-4)`
    - `.setting-row` contains `padding: 16px 0`
    - `.drive-card` contains `padding: var(--space-4)` and `gap: 8px` and `box-shadow: var(--shadow-sm)`
    - `.drive-card:hover` rule exists with `border-color: rgba(255,255,255,0.12)`
    - `.status-dot` contains `box-shadow: 0 0 0 2px var(--bg-elevated)`
    - `.error-entry` contains `padding: 16px 16px` and `box-shadow: var(--shadow-sm)`
    - `.cache-bar` contains `border-radius: var(--radius-sm)` and `background: rgba(255,255,255,0.06)`
    - `.activity-tag` contains `border-radius: var(--radius-sm)`
    - `.health-badge` contains `border-radius: var(--radius-sm)`
    - `#status-bar` contains `font-size: 11px` and `border-radius: var(--radius-md) var(--radius-md) 0 0`
    - `.stepper-item` contains `padding: 8px 16px` and `font-size: 13px` and `font-weight: 400`
    - `.step h1.step-title-lg` contains `font-size: 16px`
    - `@media (prefers-reduced-motion: reduce)` block contains `button`, `.nav-item`, `.drive-card` with `transition: none`
    - `.handler-info` class exists with `display: flex; align-items: center; gap: var(--space-2)`
    - `.pin-file-count` class exists
    - `.auth-banner-icon` class exists with `color: var(--warning)`
    - `.auth-status-row` class exists
    - `.cancel-link` class exists
    - `.wizard-actions` class exists
    - `.done-mount-list` class exists
    - `.hint` rule contains `margin-top: var(--space-2)`
  </acceptance_criteria>
  <done>All component rules match UI-SPEC specs. Typography consolidated to 4-tier scale. All new CSS migration classes created. Reduced-motion block covers all new transitions.</done>
</task>

</tasks>

<verification>
1. `grep -c 'space-3\|space-5' crates/carminedesktop-app/dist/styles.css` returns `0`
2. `grep '#1e2030' crates/carminedesktop-app/dist/styles.css` returns the :root line
3. `grep '\.handler-info' crates/carminedesktop-app/dist/styles.css` returns the new class
4. `grep '\.drive-card:hover' crates/carminedesktop-app/dist/styles.css` returns the hover rule
5. `make build` passes (no Rust compilation errors)
</verification>

<success_criteria>
- styles.css :root tokens match 04-UI-SPEC.md exactly (all 28 token values)
- --space-3 and --space-5 fully removed with zero remaining references
- Typography scale consolidated: only 11px, 13px, 16px, 32px sizes remain; only 400 and 600 weights
- All component specs (buttons, inputs, toggles, sidebar, cards, errors, badges, cache bar, status bar) match UI-SPEC
- New CSS classes for inline style migration are defined and ready for Plan 02
- @media (prefers-reduced-motion: reduce) covers all new transition properties
- `make build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-polish-hardening/04-01-SUMMARY.md`
</output>
