---
phase: 4
slug: visual-polish-hardening
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 4 -- UI Design Contract

> Visual and interaction contract for the Visual Polish & Hardening phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> This phase is a refinement pass on existing UI surfaces (settings.html, wizard.html). No new panels, no new data sources. All changes cascade through the existing CSS custom property token system in `:root`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (vanilla CSS custom properties) |
| Preset | not applicable |
| Component library | none (hand-rolled components) |
| Icon library | Inline SVG, Feather-style strokes (existing) |
| Font | Inter Variable (already bundled at `dist/fonts/InterVariable.woff2`) |

---

## Spacing Scale

All values are multiples of 4. The existing token names (`--space-1` through `--space-8`) are preserved. Values are mapped to a consistent 4-based scale, replacing the current rem-based tokens.

| Token | Current Value | New Value | Usage |
|-------|---------------|-----------|-------|
| `--space-1` | 0.25rem (4px) | 4px | Icon gaps, inline padding, tight internal gaps |
| `--space-2` | 0.5rem (8px) | 8px | Compact element spacing, button icon gap |
| `--space-3` | 0.75rem (12px) | 12px | Nav item padding, card internal padding, sidebar footer padding. Exception: 12px is not in the standard set but is retained because the existing token `--space-3` bridges the 8px-to-16px gap in a dense desktop UI where 8px is too tight for card padding and 16px is too loose. Used in 6+ component rules. |
| `--space-4` | 1rem (16px) | 16px | Default section padding, card gaps, status bar padding |
| `--space-5` | 1.25rem (20px) | 20px | Section heading margin-bottom, sidebar header bottom padding. Exception: 20px is not in the standard set but is retained because it provides visual breathing room below section headings without the full 24px gap that would create too much separation in the compact sidebar and dashboard layouts. Remapping to 16px collapses headings into content; 24px wastes vertical space in a dense data display. |
| `--space-6` | 1.5rem (24px) | 24px | Main content horizontal padding, major section gaps |
| `--space-8` | 2rem (32px) | 32px | Main content vertical padding, page-level spacing |

Exceptions:
- Touch targets on icon buttons: minimum 28px hit area (7x the base unit) for comfortable click targets on desktop. This is below the 44px mobile guideline but appropriate for a desktop-only app.
- Main content padding changes from `22px 28px` to `--space-8 --space-6` (32px 24px) for more breathing room.
- `--space-3` (12px) and `--space-5` (20px) are declared exceptions to the standard set {4, 8, 16, 24, 32, 48, 64} with rationale documented in the table above. Both are multiples of 4.

---

## Typography

Font: Inter Variable, weight range 100-900.

The existing type scale uses too many fractional pixel sizes (11px, 11.5px, 12px, 12.5px, 13px, 16px, 18px). This contract consolidates to 4 distinct sizes with 2 weights.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 13px | 400 | 1.5 | Default text, setting labels `.label-text`, activity names, error messages, cache text, mount names, pin names, button text, nav items, card names `.drive-card-name`, error file names, handler extensions, sidebar title, input/select text |
| Small / Meta | 11px | 400 | 1.4 | Timestamps, sublabels `.label-sub`, error hints, mount paths, pin meta, health badge text, activity tags, `.btn-sm` text, status bar text |
| Heading | 16px | 600 | 1.25 | Section heading overline, panel headings, step titles `h1`, wizard step-title-lg (merged into this tier at 16px, down from 18px) |
| Display | 32px | 600 | 1.1 | Reserved for future use. Not used in current UI surfaces. |

Weight system:
- 400 (regular): Body text, descriptions, metadata, nav items, button text, card names, input text
- 600 (semibold): Section headings, panel headings, step titles, active nav emphasis, health badge labels

Migration from current values:
- 12.5px button text --> 13px weight 400
- 11.5px inputs --> 13px weight 400 (folded into body tier for consistency)
- 12.5px nav items --> 13px weight 400
- 11px section headings --> 11px weight 600 (unchanged)
- 13px label text --> 13px weight 400 (unchanged)
- 18px wizard step-title-lg --> 16px weight 600 (merged into heading tier)
- 12px status bar --> 11px weight 400 (folded into small/meta tier)
- 500 weight (previously used for buttons, nav, card titles) --> collapsed into 400 for body-level elements and 600 for heading-level elements

---

## Color

Direction: **Soft dark refresh** -- lift the dark palette for more visual warmth and less cave-like heaviness. Inspired by Linear/Raycast soft dark, not stark terminal-black. `color-scheme: dark` is preserved.

**Primary visual focal point:** The drive card list is the primary visual anchor on the dashboard. Drive cards receive elevated backgrounds, subtle shadows, and hover border transitions to draw the eye and communicate interactivity. All other dashboard sections (activity, errors, cache, offline) are secondary surfaces that support the drive status overview.

### Background Tokens (60% Dominant)

| Token | Current | New | Rationale |
|-------|---------|-----|-----------|
| `--bg-base` | `#1c1d27` | `#1e2030` | Lifted from near-black. Warmer blue undertone. Still unmistakably dark. |
| `--bg-surface` | `#20222e` | `#242637` | Sidebar, secondary surfaces. Slightly lifted. |
| `--bg-elevated` | `#252733` | `#2a2d3e` | Cards, inputs, drive cards, error entries. More visible lift from surface. |

### Text Tokens

| Token | Current | New | Rationale |
|-------|---------|-----|-----------|
| `--text-primary` | `#e8e9f0` | `#e2e4f0` | Slightly softer white. Retains high readability. |
| `--text-secondary` | `#8c90aa` | `#8e92b0` | Minor warmth adjustment. Used for status text, descriptions. |
| `--text-muted` | `#5c6080` | `#6e7298` | Significant lift for better readability. Current value fails WCAG AA against dark backgrounds. New value reaches ~4.5:1 contrast ratio against `--bg-base`. |

### Border Tokens (30% Secondary)

| Token | Current | New | Rationale |
|-------|---------|-----|-----------|
| `--border` | `rgba(255,255,255,0.09)` | `rgba(255,255,255,0.08)` | Slightly softer. Used for sidebar border, card borders. |
| `--border-row` | `rgba(255,255,255,0.07)` | `rgba(255,255,255,0.05)` | More subtle row separators. Background contrast between base and elevated now carries more of the visual separation. |

### Accent Token (10%)

| Token | Current | New | Rationale |
|-------|---------|-----|-----------|
| `--accent` | `#99222E` | `#99222E` | **Unchanged.** Carmine red is brand identity. |
| `--accent-hover` | `#b52a38` | `#b52a38` | **Unchanged.** |
| `--accent-bg` | `rgba(153,34,46,0.85)` | `rgba(153,34,46,0.80)` | Slightly reduced opacity for better integration against lifted backgrounds. |
| `--accent-glow` | `0 0 20px rgba(153,34,46,0.2)` | `0 0 16px rgba(153,34,46,0.15)` | Tighter, subtler glow. |

Accent reserved for:
- Primary action buttons (accent-bg background)
- Active sidebar nav item background
- Active toggle switch track
- Focus-visible outline ring
- Sign Out link color
- Checkbox/radio selected state
- Links in attribution text
- Sidebar logo gradient

NOT for: body text, section headings, card borders, status indicators. Status uses semantic colors only.

### Semantic Colors

| Token | Current | New | Usage |
|-------|---------|-----|-------|
| `--success` | `#22c55e` | `#34d399` | Slightly warmer green. Status dot (ok), cache bar (low), health badge (downloaded), toggle checked state in wizard. |
| `--danger` | `#ef4444` | `#f87171` | Slightly softer red. Error indicators, destructive action hover, cache bar (high), health badge (stale). |
| `--warning` | `#f59e0b` | `#fbbf24` | Slightly brighter amber. Syncing status dot, conflict borders, cache bar (medium), health badge (partial), auth banner. |

### Shadow & Elevation

| Token | Current | New | Usage |
|-------|---------|-----|-------|
| `--shadow-sm` | `0 1px 3px rgba(0,0,0,0.4)` | `0 1px 2px rgba(0,0,0,0.25)` | Subtle depth on small elevated elements. |
| `--shadow-md` | `0 4px 16px rgba(0,0,0,0.5)` | `0 4px 12px rgba(0,0,0,0.35)` | Drive cards, error entries. Less heavy. |
| `--shadow-glow` | `var(--accent-glow)` | `var(--accent-glow)` | Unchanged alias. |

---

## Border Radius Scale

Direction: Rounder corners for a more modern, softer feel.

| Token | Current | New | Usage |
|-------|---------|-----|-------|
| `--radius-sm` | 4px | 6px | Error entry, small badges, activity tags |
| `--radius-md` | 6px | 8px | Buttons, inputs, nav items, drive cards, sidebar logo |
| `--radius-lg` | 10px | 12px | Modal-like containers (if any), large panels |

The toggle switch track border-radius stays at 8px (half of 16px height) for pill shape.

---

## Transition & Animation

| Property | Value | Usage |
|----------|-------|-------|
| Default duration | `0.15s` | All interactive hover/focus transitions |
| Easing | `ease` | Standard easing for all transitions (existing, unchanged) |
| Extended duration | `0.25s` | Disclosure triangle rotation, cache bar fill width |
| Spinner duration | `0.8s linear infinite` | Loading spinners (existing, unchanged) |
| Reduced motion | `@media (prefers-reduced-motion: reduce)` removes all transitions and animations | Already implemented, preserve. |

New transition properties to add on interactive elements:
- Buttons: `transition: background 0.15s ease, color 0.15s ease, box-shadow 0.15s ease;`
- Nav items: add `box-shadow` to existing transition list for hover glow effect
- Toggle tracks: already have `0.2s ease`, keep as-is
- Cards: `transition: border-color 0.15s ease, box-shadow 0.15s ease;`

---

## Component Polish Specifications

### Buttons

| Property | Current | New |
|----------|---------|-----|
| Padding (default) | `7px 18px` | `8px 16px` |
| Font size | `12.5px` | `13px` |
| Font weight | `500` | `400` |
| Border radius | `var(--radius-md)` (6px) | `var(--radius-md)` (8px, updated token) |
| `.btn-sm` padding | `var(--space-1) var(--space-3)` | `4px 12px` |
| `.btn-sm` font-size | `11px` | `11px` (unchanged) |
| `.btn-ghost` border | `1px solid rgba(255,255,255,0.10)` | `1px solid var(--border)` (use token) |

### Inputs & Selects

| Property | Current | New |
|----------|---------|-----|
| Padding | `4px 10px` | `8px 12px` |
| Font size | `11.5px` | `13px` |
| Border radius | `5px` | `var(--radius-md)` (8px) |
| Border | `1px solid rgba(255,255,255,0.10)` | `1px solid var(--border)` (use token) |

### Toggle Switches

| Property | Current | New |
|----------|---------|-----|
| Track size | `30px x 16px` | `32px x 18px` |
| Thumb size | `12px` | `14px` |
| Thumb offset (top/left) | `2px` | `2px` |
| Thumb translate | `14px` | `14px` |
| Thumb unchecked color | `var(--text-muted)` | `var(--text-secondary)` (brighter unchecked state) |
| Track unchecked background | `var(--bg-elevated)` | `rgba(255,255,255,0.08)` (slightly visible track) |

### Status Dots

| Property | Current | New |
|----------|---------|-----|
| Size | `8px` | `8px` (unchanged) |
| Additional | none | Add `box-shadow: 0 0 0 2px var(--bg-elevated)` as a subtle ring for better visibility on card backgrounds |

### Health Badges & Activity Tags

| Property | Current | New |
|----------|---------|-----|
| Padding | `4px 8px` | `4px 8px` (unchanged) |
| Border radius | `3px` | `var(--radius-sm)` (6px, rounder) |
| Font size | `11px` | `11px` (unchanged) |
| Font weight | `600` | `600` (unchanged) |

### Drive Cards

| Property | Current | New |
|----------|---------|-----|
| Padding | `12px` | `var(--space-3) var(--space-4)` (12px 16px) |
| Gap (internal) | `4px` | `8px` |
| Border | `1px solid var(--border)` | `1px solid var(--border)` (unchanged, token value softer) |
| Shadow | none | `var(--shadow-sm)` |
| Hover | none | `border-color: rgba(255,255,255,0.12)` on hover for subtle interactivity hint |

### Error Entries

| Property | Current | New |
|----------|---------|-----|
| Padding | `8px 16px` | `12px 16px` |
| Border-left | `3px solid var(--danger)` | `3px solid var(--danger)` (unchanged) |
| Border radius | `var(--radius-sm)` (4px) | `var(--radius-sm)` (6px, updated token) |
| Shadow | none | `var(--shadow-sm)` |

### Sidebar

| Property | Current | New |
|----------|---------|-----|
| Width | `190px` | `190px` (unchanged) |
| Padding | `14px 10px` | `16px 12px` |
| Nav item padding | `7px 12px` | `8px 12px` |
| Nav item gap | `2px` | `2px` (unchanged) |
| Header padding-bottom | `18px` | `20px` |
| Footer padding | `10px 12px` | `12px 12px` |

### Setting Rows

| Property | Current | New |
|----------|---------|-----|
| Padding | `9px 0` | `12px 0` |
| Border-bottom | `1px solid var(--border-row)` | `1px solid var(--border-row)` (token value is now softer) |

### Cache Bar

| Property | Current | New |
|----------|---------|-----|
| Height | `8px` | `8px` (unchanged) |
| Border radius | `4px` | `var(--radius-sm)` (6px) |
| Track background | `var(--bg-elevated)` | `rgba(255,255,255,0.06)` (more subtle track) |

---

## Inline Style Migration

The following inline styles in JS render functions must be migrated to CSS classes during this phase. This eliminates style-logic coupling and ensures the token system governs all visual properties.

| File | Function | Current Inline Style | Target CSS Class |
|------|----------|---------------------|-----------------|
| `settings.js` | `renderHandlers()` | `info.style.display = 'flex'; info.style.alignItems = 'center'; info.style.gap = '10px'` | `.handler-info { display: flex; align-items: center; gap: var(--space-2); }` |
| `settings.js` | `renderOfflinePins()` | `fileCount.style.fontSize = '11px'; fileCount.style.color = 'var(--text-muted)'; fileCount.style.marginLeft = '4px'` | `.pin-file-count { font-size: 11px; color: var(--text-muted); margin-left: var(--space-1); }` |
| `settings.js` | `renderDashboard()` | `icon.style.color = 'var(--warning)'` | `.auth-banner-icon { color: var(--warning); }` |
| `wizard.html` | Various | `style="display:flex;align-items:center;gap:10px;margin-bottom:20px"` | `.auth-status-row { display: flex; align-items: center; gap: var(--space-2); margin-bottom: var(--space-5); }` |
| `wizard.html` | Various | `style="margin-top:var(--space-6);display:flex;flex-direction:column;align-items:center;gap:var(--space-2)"` | `.wizard-actions { margin-top: var(--space-6); display: flex; flex-direction: column; align-items: center; gap: var(--space-2); }` |
| `wizard.html` | Various | `style="margin-top:8px"` | `.hint { margin-top: var(--space-2); }` (update existing rule) |
| `wizard.html` | Various | `style="margin-top:20px"` | `.cancel-link { margin-top: var(--space-5); }` |

---

## Copywriting Contract

This phase does not introduce new panels or features. Copywriting contract covers existing copy that the executor must preserve, plus any new feedback messages for UI-02.

| Element | Copy |
|---------|------|
| Primary CTA (wizard) | "Sign In with Microsoft" (existing, unchanged) |
| Dashboard empty state (no drives) | "No drives mounted" (existing, unchanged) |
| Dashboard empty state (no activity) | "No recent activity" (existing, unchanged) |
| Dashboard empty state (no errors) | "No errors" (existing, unchanged) |
| Dashboard empty state (no pins) | "No offline pins" (existing, unchanged) |
| Mounts empty state | "No mounts configured" (existing, unchanged) |
| Offline pins empty state | "No folders pinned for offline use" (existing, unchanged) |
| Error state (generic) | Handled by `formatError()` in `ui.js` with pattern matching. Existing patterns preserved. |
| Destructive: Remove mount | Confirmation dialog: "Remove this mount? This cannot be undone." (existing, unchanged) |
| Destructive: Sign out | Confirmation dialog: "Sign out? All mounts will stop." (existing, unchanged) |
| Destructive: Clear cache | Button label "Clear Cache" with `showStatus('Cache cleared', 'success')` feedback. Label updated from bare "Clear" to include noun for clarity. |
| Destructive: Remove pin | No confirmation dialog (existing behavior -- lightweight action). Feedback: "Unpinned {name}" via `showStatus()` (existing). |

### UI-02 Action Feedback Audit

Every user-initiated action must call `showStatus()` on completion. The following is the audit of all actions with their current feedback status:

| Action | File | Current Feedback | Status |
|--------|------|------------------|--------|
| Toggle mount | `settings.js` | `showStatus('Mount updated', 'success')` | OK |
| Remove mount | `settings.js` | `showStatus('Mount removed', 'success')` | OK |
| Add mount (open wizard) | `settings.js` | Error only via `showStatus()` | OK (wizard opens as feedback) |
| Sign out | `settings.js` | `showStatus('Signed out', 'success')` | OK |
| Clear cache | `settings.js` | `showStatus('Cache cleared', 'success')` | OK |
| Remove pin | `settings.js` | `showStatus('Unpinned {name}', 'success')` | OK |
| Re-detect handlers | `settings.js` | `showStatus('Handlers re-detected', 'success')` | OK |
| Save handler override | `settings.js` | `showStatus('Handler override saved for {ext}', 'success')` | OK |
| Clear handler override | `settings.js` | `showStatus('Handler override cleared for {ext}', 'success')` | OK |
| Save settings | `settings.js` | Error only. No success feedback on auto-save. | OK (silent auto-save is appropriate -- no action needed) |
| Sign in (wizard) | `wizard.js` | Step navigation is feedback. Errors shown. | OK |
| Copy auth URL | `wizard.js` | Button text changes to "Copied!" for 2s | NEEDS FIX: button text says "Copy" but should say "Copy URL" initially for clarity. The post-copy text "Copied!" is fine. |
| Cancel sign-in | `wizard.js` | Returns to welcome step. No `showStatus()`. | OK (navigation is feedback) |
| Add SharePoint libraries | `wizard.js` | `showStatus('Library added successfully', 'success')` | OK |
| Remove added source | `wizard.js` | Error only. No success feedback. | NEEDS FIX: Add `showStatus('Source removed', 'success')` |
| Get started (complete wizard) | `wizard.js` | Step navigation to success screen. | OK |
| Switch account | `wizard.js` | Button text changes during operation. Returns to welcome. | OK |
| Dashboard sign in (auth banner) | `settings.js` | `showStatus('Please sign in again', 'info')` | OK |

**Actions requiring fixes for UI-02 compliance:**
1. `wizard.js` `copyAuthUrl()`: Change initial button text from "Copy" to "Copy URL"
2. `wizard.js` `removeSource()`: Add `showStatus('Source removed', 'success')` after successful removal

---

## Wizard Page Treatment

The wizard (`wizard.html`, `wizard.js`) receives **proportional visual updates** during this phase:

- Same token changes cascade automatically (backgrounds, text colors, borders, radii, shadows)
- Button styles, input styles, toggle styles all inherit from shared CSS rules
- Stepper items follow the updated nav item patterns
- Inline styles on wizard HTML get migrated to CSS classes (see Inline Style Migration table)
- No structural changes to wizard flow or step sequence

---

## Status Bar Refinement

| Property | Current | New |
|----------|---------|-----|
| Font size | `12px` | `11px` (folded into small/meta tier) |
| Padding | `var(--space-2) var(--space-4)` | `var(--space-2) var(--space-4)` (unchanged) |
| Success background | `var(--success)` | `var(--success)` (new value `#34d399`) |
| Error background | `var(--danger)` | `var(--danger)` (new value `#f87171`) |
| Info background | `var(--bg-elevated)` | `var(--bg-elevated)` (new value `#2a2d3e`) |
| Border radius | none | `var(--radius-md) var(--radius-md) 0 0` (top corners rounded) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | not applicable | not applicable |

This project uses vanilla CSS with no component registry, no npm UI packages, and no third-party design blocks. All components are hand-rolled. Registry safety gate is not applicable.

---

## Cross-Platform Notes

- Font rendering: Inter Variable with `-webkit-font-smoothing: antialiased` on Linux/macOS, Segoe UI fallback on Windows. No additional cross-platform font adjustments needed -- existing font stack handles this.
- CSS `color-scheme: dark` informs OS-level form controls (scrollbars, select dropdowns). Preserved.
- Layout: Flexbox-based, no CSS Grid. Works identically across Chromium (Tauri webview) on all platforms.
- All changes are token-level. No platform-gated CSS needed.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
