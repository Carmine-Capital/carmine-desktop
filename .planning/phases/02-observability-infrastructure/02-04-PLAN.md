---
phase: 02-observability-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "invoke('get_dashboard_status') returns valid JSON from the browser console with per-drive data"
    - "invoke('get_recent_errors') returns valid JSON (empty array if no errors yet)"
    - "invoke('get_activity_feed') returns valid JSON (empty array or entries if delta sync ran)"
    - "invoke('get_cache_stats') returns valid JSON with diskUsedBytes, diskMaxBytes, memoryEntryCount, pinnedItems, writebackQueue"
    - "listen('obs-event') receives events when delta sync runs or VFS errors occur"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 observability infrastructure from the browser console. All four Tauri commands must return correct JSON, and real-time events must be observable via listen().

Purpose: This is the final verification that ties together Plans 01-03. The success criteria are defined as "verifiable from the browser console" — this checkpoint confirms that.

Output: Phase 2 verified complete, ready for Phase 3 (Dashboard UI).
</objective>

<execution_context>
@/home/nyxa/.claude/get-shit-done/workflows/execute-plan.md
@/home/nyxa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-observability-infrastructure/02-CONTEXT.md
@.planning/phases/02-observability-infrastructure/02-UI-SPEC.md
@.planning/phases/02-observability-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-observability-infrastructure/02-02-SUMMARY.md
@.planning/phases/02-observability-infrastructure/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Browser console validation of all Tauri dashboard commands and real-time events</name>
  <files>none</files>
  <action>
    This is a human verification checkpoint. The executor presents the verification steps to the user and waits for approval.

    What was built: Complete observability data layer — 4 Tauri commands (get_dashboard_status, get_recent_errors, get_activity_feed, get_cache_stats) and real-time ObsEvent emission via Tauri emit().

    Verification steps for the user:

    Prerequisites: App running on host with at least one drive mounted.

    1. Open the app's WebView dev tools (right-click -> Inspect, or Ctrl+Shift+I if enabled)
    2. In the browser console, run each command:

    SC-1 Dashboard status:
    const status = await window.__TAURI__.core.invoke('get_dashboard_status');
    console.log(JSON.stringify(status, null, 2));
    Expected: JSON with drives array (driveId, name, mountPoint, online, lastSynced, syncState, uploadQueue), authenticated: true, authDegraded: false.

    SC-2 Recent errors:
    const errors = await window.__TAURI__.core.invoke('get_recent_errors');
    console.log(JSON.stringify(errors, null, 2));
    Expected: Array (empty if no errors). Each error has driveId, fileName, remotePath, errorType, message, actionHint, timestamp.

    SC-3 Cache stats:
    const stats = await window.__TAURI__.core.invoke('get_cache_stats');
    console.log(JSON.stringify(stats, null, 2));
    Expected: Object with diskUsedBytes, diskMaxBytes, memoryEntryCount, pinnedItems array, writebackQueue array.

    SC-3b Activity feed:
    const activity = await window.__TAURI__.core.invoke('get_activity_feed');
    console.log(JSON.stringify(activity, null, 2));
    Expected: Array (empty or entries with driveId, filePath, activityType, timestamp).

    SC-4 Real-time events:
    await window.__TAURI__.event.listen('obs-event', e => console.log('obs-event:', e.payload));
    await window.__TAURI__.event.listen('activity-batch', e => console.log('batch:', e.payload.length));
    Wait ~60s for delta sync cycle, then check console for events.

    3. Verify all JSON field names are camelCase (driveId not drive_id)
    4. Verify diskUsedBytes and diskMaxBytes are sensible numbers

    Resume signal: Type "approved" if all 4 commands return correct JSON and events appear, or describe issues.
  </action>
  <verify>Manual verification from browser console — user confirms all 4 success criteria pass</verify>
  <done>All four Tauri commands return valid camelCase JSON from browser console, and real-time events are receivable via listen()</done>
</task>

</tasks>

<verification>
All four Phase 2 success criteria verified from browser console:
1. get_dashboard_status returns per-drive sync state, online/offline, last synced, auth health
2. get_recent_errors returns errors with file name, error type, timestamp
3. get_cache_stats returns disk usage vs max, pin count, writeback queue
4. Real-time events pushed via emit(), received by listen()
</verification>

<success_criteria>
- All four Tauri commands return valid JSON from browser console
- JSON field names are camelCase per UI-SPEC contract
- Real-time events observable via listen('obs-event') and listen('activity-batch')
- Phase 2 complete, Phase 3 (Dashboard UI) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/02-observability-infrastructure/02-04-SUMMARY.md`
</output>
